@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
    
}



<div class="container mt-4 mb-4">
    <div class="row">
        <div class="col-6">
            <h2>Category Index</h2>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-asp-controller="Category" asp-action="Create">
                <i class="bi bi-plus-square"></i>
                Crear Categoria
            </a>
        </div>
    </div>
</div>



<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Order de Visualización</th>
            <th>Editar</th>
            <th>Eliminar</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var category in Model)
        {
            <tr>
                <td style="width: 50%">@category.Name</td>
                <td style="width: 10%">@category.DisplayOrder</td>
                <td style="width: 10%">
                    <a class="btn btn-warning" asp-asp-controller="Category" asp-action="Edit" asp-route-id="@category.Id">
                        <i class="bi bi-pencil"></i>Editar
                    </a>
                </td>
                <td style="width: 10%">
                    <a class="btn btn-danger" asp-asp-controller="Category" asp-action="Delete" asp-route-id="@category.Id">
                        <i class="bi bi-trash"></i>Eliminar
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

